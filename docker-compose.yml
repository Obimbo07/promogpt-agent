# Local self-host scaffold — wire Supabase + Stripe secrets via `.env` or your secret manager.

services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
